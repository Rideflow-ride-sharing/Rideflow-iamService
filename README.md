# Rideflow-iamService

Manages user and driver identities. Handles registration, login, and JWT token issuance. Stores hashed credentials and profile data. All other services trust tokens issued by IAM — they don't verify users themselves.
